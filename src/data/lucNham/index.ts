/**
 * Local static data for Lục Nhâm (六壬) — lookup tables and rule bundles.
 * Add exports here as tables are transcribed from project documentation (no network).
 */

export const LUC_NHAM_DATA_VERSION = '0.1.0';
